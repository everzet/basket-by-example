default:
    suites:
        web:
            contexts:
                - WebBasketContext:
                    dbFile: '%paths.base%/db_test.sqlite'

    extensions:
        Behat\MinkExtension:
            base_url: 'http://localhost:8000/index_test.php'
            sessions:
                default:
                    selenium2: { browser: 'chrome' }
