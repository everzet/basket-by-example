default:
    suites:
        web:
            contexts: [ 'WebBasketContext' ]

    extensions:
        Behat\MinkExtension:
            base_url: 'http://localhost:8000'
            sessions:
                default:
                    selenium2: { browser: 'chrome' }
