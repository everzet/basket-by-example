default:
    suites:
        web:
            filters:
                tags: '@e2e'
            contexts:
                - WebBasketContext: [ 'test' ]
        domain:
            contexts:
                - DomainBasketContext

    extensions:
        Behat\MinkExtension:
            base_url: 'http://localhost:8000/index_test.php'
            sessions:
                default:
                    selenium2: { browser: 'chrome' }
